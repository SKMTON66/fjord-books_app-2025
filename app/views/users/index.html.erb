<% content_for :title, "Users" %>

<h1>Users</h1>

<div id="users">
  <% @users.each do |user| %>
    <%= user.username %>
    <%= user.email %>
    <p>
      <%= link_to "Show this user", user %>
    </p>
  <% end %>
</div>

<%= paginate @users %>

<%= link_to "books", books_path %>
